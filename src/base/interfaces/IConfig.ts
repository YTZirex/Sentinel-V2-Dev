export default interface IConfig {
  token: string;
  clientId: string;
  devGuildId: string;
  devIds: string;
  devToken: string;
  devClientId: string;
  devMongoURI: string;
  mongoURI: string;
  botVersion: string;
}
